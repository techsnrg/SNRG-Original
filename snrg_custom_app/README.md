# SNRG Custom

Clean replacement app scaffold for rebuilding the useful parts of the legacy `snrg` app.

This folder is intentionally separate from the legacy app so we can migrate feature-by-feature without disturbing the old package.
