[project]
name = "snrg_custom"
authors = [
    { name = "administrator", email = "hello@aerele.in" }
]
description = "Clean SNRG customizations for ERPNext"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]

[tool.bench.frappe-dependencies]
frappe = ">=16.0.0-dev,<17.0.0"
erpnext = ">=16.0.0-dev,<17.0.0"
india_compliance = ">=16.0.0-dev,<17.0.0"
